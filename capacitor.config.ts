import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.marketplace.member.of.kingdom.capacitor',
  appName: 'Member of Kingdom',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
